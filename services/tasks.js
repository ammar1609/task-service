const accept = require("./accept");
const reject = require("./reject");
const borrow = require("./borrow");
const returnFn = require("./return");

module.exports = {
  borrow,
  accept,
  reject,
  returnFn,
};
